import { Route } from '@angular/router';

import { RandomMovieComponent } from './random-movie.component';

export const RANDOM_ROUTE: Route = {
  path: 'random',
  component: RandomMovieComponent,
};
